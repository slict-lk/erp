import { AIModuleLayout } from '@/components/ai/ai-module-layout';

export default function AIWorkspaceLayout({ children }: { children: React.ReactNode }) {
  return <AIModuleLayout>{children}</AIModuleLayout>;
}
